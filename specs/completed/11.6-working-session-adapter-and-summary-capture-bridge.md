# Session 11.6 - Working Session Adapter And Summary Capture Bridge

Status: Completed
Owner: Codex
Date: 2026-06-03

## Why This Session Exists

Session 11.5 established HAETSAL's owned working-session boundary:

- transcript entries are volatile and non-canonical
- scratch notes are volatile, bounded, and non-canonical
- context bundles are read-only and non-canonical
- explicit session-close summaries are evidence candidates
- canonical truth remains Postgres/R2 through Hyperdrive

The next narrow step is an internal adapter/capture bridge that can accept
working-session summary evidence emissions and hand their existing
`ExternalClientCaptureInput` shape to a dependency-injected capture sink.

This phase does not wire production agents, tools, routes, bindings, Durable
Objects, or deployment configuration to the new abstraction.

## Goal

Add a production-neutral seam for session-summary capture:

1. define a small working-session adapter interface
2. define a capture sink interface
3. accept `WorkingSessionCanonicalEvidenceEmission[]`
4. send only explicit session-close summary emissions to the sink
5. report captured, skipped, and failed emissions explicitly
6. preserve the HAETSAL write-policy boundary in sink metadata and bridge
   result state

## Boundary

The bridge is an adapter seam, not a runtime integration.

- It imports only local types from the working-session and external-client
  capture contracts.
- It does not import or call live `Env`, MCP context, Durable Objects,
  Cloudflare Sessions, Hindsight, or canonical store implementations.
- It does not import or call `captureExternalClientMemory`.
- It does not change canonical memory behavior.
- It does not register new MCP tools or routes.
- It does not add Cloudflare bindings or Durable Object migrations.
- It does not wire `BaseAgent`, `McpAgentDO`, or production agents to the new
  abstraction.

Only emissions that still prove the explicit summary contract are accepted:

- `kind = "canonical_evidence_emission"`
- `source = "session_close_summary"`
- `summary.kind = "session_close_summary"`
- `summary.canonicalDisposition = "session_summary_evidence_candidate"`
- `summary.requiresHaetsalWritePolicy = true`
- `captureMode = "session_summary"`
- `capture.capture_mode = "session_summary"`
- `writePolicy = "haetsal_write_policy_required"`
- `canonicalTruthBoundary = "postgres_r2_hyperdrive"`

Everything else is skipped before the sink is called.

## Implementation

Added `src/services/working-session-capture-bridge.ts`.

The module defines:

- `WorkingSessionAdapter`
- `WorkingSessionEvidenceCaptureSink`
- `WorkingSessionEvidenceCaptureMetadata`
- `WorkingSessionEvidenceCapturedResult`
- `WorkingSessionEvidenceSkippedResult`
- `WorkingSessionEvidenceFailedResult`
- `WorkingSessionEvidenceCaptureBatchResult`
- `captureWorkingSessionEvidenceEmissions()`
- `captureWorkingSessionAdapterEvidence()`

The sink contract accepts:

- the existing external-client capture input shape
- narrowed to `capture_mode = "session_summary"`
- plus metadata that carries session id, summary id, source, capture mode,
  write-policy boundary, canonical-truth boundary, and
  `requiresHaetsalWritePolicy`

The bridge returns separate `captured`, `skipped`, and `failed` arrays in
addition to the ordered `results` array. Sink failures are reported as failed
results with `acceptedBySink = false`, not promoted to captured results.

## Tests

Added
`tests/11.6-working-session-adapter-and-summary-capture-bridge.test.ts`.

The tests prove:

- raw transcript entries, scratch notes, and read-only context bundles do not
  produce captured sink calls
- only explicit session-close summary evidence reaches the fake sink
- emitted sink input keeps `capture_mode = "session_summary"`
- write-policy and canonical-truth boundaries are preserved in metadata and
  result state
- invalid emissions are skipped before the sink is called
- fake sink failures are reported without accepting failed captures
- the bridge exposes only dependency-injected runtime functions

## Explicit Non-Changes

- Did not adopt Cloudflare Sessions.
- Did not import `agents/experimental/memory/session`.
- Did not adopt Think, AI Chat, Shell, or workers-ai-provider.
- Did not remove Hindsight.
- Did not deploy.
- Did not change `wrangler.toml`.
- Did not change MCP routes or tools.
- Did not change Durable Object migrations.
- Did not change production agent wiring.
- Did not change canonical memory behavior.

## Validation

Starting baseline:

- Ran `git fetch origin`.
- Confirmed `HEAD == origin/master`.
- Both resolved to `88cd959b8b4a5b1f42b4f85f43ed7ba73130f89c`.
- Confirmed `88cd959` is an ancestor of `HEAD`.
- Left pre-existing `.omx`, reference clone, and planning-doc noise unstaged.

Focused test:

- `npx vitest run tests/11.6-working-session-adapter-and-summary-capture-bridge.test.ts`
  - passed
  - 1 test file passed
  - 5 tests passed

Postflight:

- `npm run postflight`
  - passed
  - no violations found

Full tests:

- `npm test`
  - passed
  - 75 test files passed
  - 421 tests passed
  - 1 skipped

Guardrails:

- Runtime/package guardrail scan over `package.json`, `src`, `tests`, and
  `wrangler.toml`
  - found no forbidden session/chat/shell/provider references
- Protected diff checks for `wrangler.toml`, MCP routes/tools,
  Durable Object code, production agents, and existing canonical memory service
  files
  - found no diff
- Secret scan over `.omx/logs` and `SESSION_LOG.md`
  - files scanned: 16
  - User-scope token/access secret values checked: 4
  - exact User-scope token/access hits: 0
  - PostgreSQL URL-shaped hits: 0
  - tracked files redacted: 0

## As-Built Record

Completed: 2026-06-03

Files added:

- `src/services/working-session-capture-bridge.ts`
- `tests/11.6-working-session-adapter-and-summary-capture-bridge.test.ts`
- `specs/completed/11.6-working-session-adapter-and-summary-capture-bridge.md`

Result:

- HAETSAL now has a production-neutral bridge from working-session summary
  evidence emissions to an injected capture sink.
- The bridge keeps raw transcript entries, scratch notes, and read-only
  context bundles non-canonical by accepting only canonical evidence emissions
  and revalidating the explicit session-close summary contract before sink
  capture.
- The bridge preserves HAETSAL write-policy and canonical-truth boundaries as
  metadata/result state.
- Failed sink calls are reported as failures and are not accepted as captures.
- No production runtime behavior changed.

## Validation Review

Validated: 2026-06-03
Reviewer: Codex

Repository state:

- Confirmed `HEAD` and `origin/master` both resolve to
  `52ebc72c89a88f704be1e4977acada7c849a74ab`.
- Confirmed the 11.6 diff only adds:
  - `src/services/working-session-capture-bridge.ts`
  - `tests/11.6-working-session-adapter-and-summary-capture-bridge.test.ts`
  - this completed spec.
- Left pre-existing `.omx` runtime state, reference clones, and planning-doc
  noise unstaged.

Implementation review:

- Confirmed the bridge exports only dependency-injected runtime functions:
  `captureWorkingSessionEvidenceEmissions` and
  `captureWorkingSessionAdapterEvidence`.
- Confirmed the bridge imports only local working-session and external-client
  capture types.
- Confirmed it does not import or call live `Env`, MCP context, Durable
  Objects, Cloudflare Sessions, Hindsight, canonical stores, or
  `captureExternalClientMemory`.
- Confirmed skipped, captured, and failed result states preserve the
  session-summary, HAETSAL write-policy, and Postgres/R2/Hyperdrive canonical
  truth boundaries.

Dependency and runtime guardrails:

- `npm ls agents ai zod @cloudflare/think @cloudflare/ai-chat @cloudflare/shell workers-ai-provider --depth=0`
  - `agents@0.13.3`
  - `ai@6.0.116`
  - `zod@4.4.3`
  - no installed `@cloudflare/think`, `@cloudflare/ai-chat`,
    `@cloudflare/shell`, or `workers-ai-provider`
- Fixed-string runtime scans over `package.json`, `src`, `tests`, and
  `wrangler.toml` found no references to:
  - `agents/experimental/memory/session`
  - `@cloudflare/think`
  - `@cloudflare/ai-chat`
  - `@cloudflare/shell`
  - `workers-ai-provider`
  - `Cloudflare Sessions`
- Confirmed `wrangler.toml` has no 11.6 diff.
- Confirmed no 11.6 diff touched MCP routes/tools, Durable Object code,
  production agents, existing canonical memory services, `package.json`, or
  lockfile dependencies.
- Existing Hindsight references remain present in the broader repository, but
  11.6 did not add or expand Hindsight integration.

Validation commands:

- `npm run postflight`
  - passed
  - no violations found
- `npx vitest run tests/11.6-working-session-adapter-and-summary-capture-bridge.test.ts`
  - passed
  - 1 test file passed
  - 5 tests passed
- `npm test`
  - passed
  - 75 test files passed
  - 421 tests passed
  - 1 skipped

Secret hygiene:

- Scanned `.omx/logs/*.jsonl` and `SESSION_LOG.md`.
- Exact User-scope Cloudflare/access secret hits: 0.
- PostgreSQL URL-shaped hits: 0.

Validation conclusion:

- Session 11.6 is accepted as implemented.
- The work adds the intended production-neutral bridge from explicit
  working-session close-summary evidence to an injected capture sink.
- No production wiring, Cloudflare binding, Durable Object migration, MCP route,
  MCP tool, package, or deployment behavior changed in this phase.
