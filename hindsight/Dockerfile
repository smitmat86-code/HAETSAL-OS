# Hindsight Container — production API-only runtime
# Pinned to 0.5.2 (2026-04-17) while internal-worker mode remains the stable production path.
# Upstream release commit: 712a862 (v0.5.2 release tag)
# The worker container class overrides the entrypoint to `hindsight-worker`.

FROM ghcr.io/vectorize-io/hindsight-api:0.5.2
