# Hindsight Container — production API-only runtime
# Pinned to 0.5.3 for the latest worker fairness/reliability fixes and faster fail-under-throttle defaults.
# Upstream release tag: v0.5.3
# The worker container class overrides the entrypoint to `hindsight-worker`.

FROM ghcr.io/vectorize-io/hindsight-api:0.5.3
