# Spec: Hyperdrive Resource and Deploy Readiness

**Phase:** 10 | **Session:** 10.5 | **Priority:** P0
**Track:** Post-Hindsight Cloudflare/Open Brain Roadmap
**Implementation Tree:** `C:\Users\matth\Documents\HAETSAL OS`
**Baseline Branch:** `master`
**Depends on:** `10.4` Hyperdrive Neon canonical connection

---

## What We're Building

Finish the live Cloudflare side of the canonical Neon/Postgres Hyperdrive
cutover by ensuring the production Wrangler binding points at a real
Cloudflare Hyperdrive configuration.

This session must not change canonical memory semantics, schemas, graph
projection, consolidation behavior, Hindsight behavior, AI Search behavior, or
runtime memory read/write policy.

---

## Required Reading

Completed before execution:

- `specs/completed/10.4-hyperdrive-neon-canonical-connection.md`
- `docs/implementation-plans/canonical-worktree-consolidation-report.md`
- `docs/implementation-plans/cloudflare-runtime-currency-report.md`

Cloudflare references checked on 2026-06-01:

- Hyperdrive Neon guide:
  `https://developers.cloudflare.com/hyperdrive/examples/connect-to-postgres/postgres-database-providers/neon/`
- Hyperdrive Wrangler commands:
  `https://developers.cloudflare.com/workers/wrangler/commands/hyperdrive/`
- Cloudflare Hyperdrive API list endpoint:
  `https://developers.cloudflare.com/api/resources/hyperdrive/subresources/configs/methods/list/`
- Wrangler deploy command:
  `https://developers.cloudflare.com/workers/wrangler/commands/workers/#deploy`

---

## Current Baseline

- Local `HEAD` must equal `origin/master`.
- Local `HEAD` must be `e3105cb` or newer.
- `wrangler.toml` currently has `[[hyperdrive]]` binding
  `HYPERDRIVE_CANONICAL`.
- Before this spec, that binding still used
  `id = "replace-with-hyperdrive-canonical-id"`.
- The canonical runtime connection resolver from 10.4 remains:
  1. `env.HYPERDRIVE_CANONICAL.connectionString`
  2. `env.CANONICAL_POSTGRES_CONNECTION_STRING`
  3. throw a canonical Postgres configuration error
- `NEON_CONNECTION_STRING` remains Hindsight-owned and must not be reused for
  canonical Hyperdrive creation.

---

## Brain Laws Check

### Law 1: One Public Face

No new public routes, MCP tools, or user-facing brain surfaces.

### Law 2: Zero-Knowledge

Do not print, log, commit, or expose the canonical Neon/Postgres URL. Do not
add plaintext memory storage, cache, Analytics, KV, D1, or AI Gateway payload
retention.

### Law 3: Agent Write Policy

No change to source authority, write policy, salience, review, or capture
rules.

### Law 4: Post-Hindsight Direction

No Hindsight removal in this spec. Hindsight keeps its existing
`NEON_CONNECTION_STRING` container path.

### Law 5: Canonical Cloudflare Data Path

HAETSAL-owned canonical Postgres access should use `HYPERDRIVE_CANONICAL` in
production. Direct canonical connection strings are local/fallback only and
must not fall back to the Hindsight-owned `NEON_CONNECTION_STRING`.

---

## Scope

### In Scope

1. Verify local `HEAD` equals `origin/master` and is `e3105cb` or newer.
2. Inspect whether a real Cloudflare Hyperdrive config already exists for the
   canonical Neon/Postgres database.
3. If a real config exists, update `wrangler.toml` to replace
   `replace-with-hyperdrive-canonical-id`.
4. If no real config exists, create it with the canonical unpooled
   Neon/Postgres URL.
5. Run:
   - `npm run cf:types`
   - focused Hyperdrive/canonical tests
   - `npm run postflight`
   - `npm test`
6. Run a Wrangler deploy dry run with the safest container strategy available,
   expected to be `--containers-rollout=none` when Docker is unavailable.
7. Append this As-Built record and move the spec to completed.

### Out of Scope

- No Hindsight removal.
- No AI Search.
- No graph, consolidation, or memory behavior changes.
- No live deploy unless explicitly approved.
- No use of `NEON_CONNECTION_STRING` for canonical Hyperdrive creation.

---

## Implementation Plan

1. Fetch `origin` and verify the branch gate.
2. Inspect local env and project env files for a canonical, unpooled
   Neon/Postgres URL without printing values.
3. List Cloudflare Hyperdrive configs without printing any database URL.
4. Create or reuse the canonical Hyperdrive config.
5. Patch `wrangler.toml` only if a real Hyperdrive config id is available.
6. Regenerate Cloudflare types and run the focused and broad verification set.
7. Run deploy dry run with `--dry-run --containers-rollout=none`.
8. Record the result here and move this spec to completed.

---

## Acceptance Criteria

- `HEAD == origin/master`.
- `HEAD` is `e3105cb` or newer.
- `HYPERDRIVE_CANONICAL` points at a real Cloudflare Hyperdrive config id, or
  execution stops with the exact missing env var/secret required.
- No canonical Neon/Postgres URL appears in source, docs, logs, command output
  summaries, or commits.
- No memory semantics change.
- `npm run cf:types` passes after any Wrangler config change.
- Focused tests pass.
- `npm run postflight` passes.
- `npm test` passes.
- Wrangler deploy dry run passes or records a non-code blocker.
- No live deploy is performed.

---

## As-Built Record

Completed: 2026-06-01

Result:

- Initial execution halted at the explicit missing-canonical-URL stop condition.
- Resumed after the required local user env vars were made available outside
  the repository:
  - `CLOUDFLARE_API_TOKEN`
  - `CANONICAL_POSTGRES_CONNECTION_STRING`
- Created the Cloudflare Hyperdrive config `haetsal-canonical-neon`.
- Set Hyperdrive query caching disabled for the canonical memory path so
  Hyperdrive does not introduce read caching semantics.
- Confirmed the Hyperdrive config uses `sslmode=require`.
- Updated `wrangler.toml` so `HYPERDRIVE_CANONICAL` points at:
  `facdb64643d74ed2b3a3b90e7a4e2071`.
- No runtime code, memory behavior, generated runtime code, Hindsight path,
  AI Search binding, graph path, consolidation path, or schema behavior was
  changed.
- No live deploy was performed.

Verified:

- Ran `git fetch origin`.
- Confirmed `HEAD` equals `origin/master`.
- Confirmed both resolve to
  `e3105cb8e2ca5d43df88a5d5ca45581240102c2c`.
- Confirmed `e3105cb` is an ancestor of `HEAD`.
- Confirmed Wrangler version `4.95.0`.
- Confirmed the current Wrangler CLI supports:
  - `wrangler hyperdrive create <name> --connection-string=...`
  - `--binding`
  - `--update-config`
  - `wrangler deploy --dry-run`
  - `wrangler deploy --containers-rollout=none`
- Confirmed the Cloudflare API can inspect Hyperdrive configs with the provided
  token.
- Confirmed no project env file was created for the canonical Neon/Postgres
  URL.
- Confirmed Cloudflare Worker secret names still include
  `NEON_CONNECTION_STRING`, which remains Hindsight-owned and was not used.
- Confirmed Cloudflare API Hyperdrive create initially rejected
  `scheme = "postgresql"` with a 400 response; creation succeeded after
  normalizing the API origin scheme to `postgres`.
- Confirmed the created config id is
  `facdb64643d74ed2b3a3b90e7a4e2071`.
- Confirmed the created config has caching disabled.
- Confirmed the created config has `sslmode=require`.

Verification:

- `npm run cf:types` - passed.
- `npx vitest run tests/10.4-hyperdrive-neon-canonical-connection.test.ts tests/10.0-canonical-postgres-source-of-truth-cutover.test.ts tests/11.0-haetsal-compiled-synthesis-foundation.test.ts tests/11.3-chief-of-staff-compiled-read-path.test.ts tests/11.4-connector-driven-compilation-triggers.test.ts`
  - passed: 5 files, 19 tests.
- `npm run postflight` - passed: `All checks passed - no violations found.`
- `npm test` - passed: 72 files, 410 passed, 1 skipped.
- `npx wrangler deploy --dry-run --containers-rollout=none` - passed; Wrangler
  bundled the Worker, recognized
  `env.HYPERDRIVE_CANONICAL (facdb64643d74ed2b3a3b90e7a4e2071)`, listed the
  configured containers, and exited before upload because `--dry-run` was set.

Notes:

- The canonical Neon/Postgres URL was not printed, committed, or written to any
  project file.
- The pasted Cloudflare token and database URL should still be treated as
  exposed credentials and rotated.

Validation review:

- Rechecked on 2026-06-01 from `C:\Users\matth\Documents\HAETSAL OS`.
- Confirmed local `HEAD` equals `origin/master` before committing this 10.5
  record, and both resolved to `e3105cb8e2ca5d43df88a5d5ca45581240102c2c`.
- Confirmed `wrangler.toml` now binds `HYPERDRIVE_CANONICAL` to
  `facdb64643d74ed2b3a3b90e7a4e2071`.
- Confirmed `CLOUDFLARE_API_TOKEN` is present in the current process without
  printing its value.
- Current Wrangler auth could not independently re-read the live Hyperdrive
  config with `wrangler hyperdrive list` or
  `wrangler hyperdrive get facdb64643d74ed2b3a3b90e7a4e2071`; Cloudflare
  returned authentication error code `10000` for those Hyperdrive API
  endpoints. This means the repo/config/deploy-dry-run state was validated in
  this review, but live Hyperdrive resource inspection was not reproducible
  with the token visible to this process.
- Re-ran `npm run cf:types`; passed.
- Re-ran the focused and adjacent Vitest set; passed with 5 files and 19 tests.
- Re-ran `npm run postflight`; passed.
- Re-ran full `npm test`; passed with 72 files, 410 passed, and 1 skipped.
- Re-ran `npx wrangler deploy --dry-run --containers-rollout=none`; passed and
  listed `env.HYPERDRIVE_CANONICAL (facdb64643d74ed2b3a3b90e7a4e2071)`.
