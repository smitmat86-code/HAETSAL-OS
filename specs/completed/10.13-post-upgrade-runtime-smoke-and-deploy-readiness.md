# Spec: Post-Upgrade Runtime Smoke and Deploy-Readiness Validation

**Phase:** 10 | **Session:** 10.13 | **Priority:** P0
**Track:** Post-Hindsight Cloudflare/Open Brain Roadmap
**Implementation Tree:** `C:\Users\matth\Documents\HAETSAL OS`
**Baseline Branch:** `master`
**Depends on:** `10.12` Zod 4 and Current Agents SDK Upgrade

---

## What We Validated

Validate the HAETSAL runtime surface after the Zod 4 plus current Cloudflare
Agents SDK upgrade, without adopting Sessions, Think, AI Chat, Shell,
workers-ai-provider, or new memory/session behavior.

This phase does not deploy.

---

## Starting Baseline

Checked on 2026-06-02 from `C:\Users\matth\Documents\HAETSAL OS`.

- `git fetch origin` completed.
- `HEAD` equals `origin/master`.
- Both resolve to `f451c254218c85e8c13e9860f690ad940b6399b2`.
- `f451c25` is an ancestor of `HEAD`.
- The Windows User-scope `CLOUDFLARE_API_TOKEN` was loaded into process env
  for Cloudflare/Wrangler checks without printing the token.
- Pre-existing `.omx` runtime state, local reference clones, and planning-doc
  noise were left untouched.

---

## Runtime and Package Validation

Installed package surface:

- `agents@0.13.3`
- `zod@4.4.3`
- `ai@6.0.116`, still AI SDK v6

Excluded packages:

- `npm ls @cloudflare/think @cloudflare/ai-chat @cloudflare/shell workers-ai-provider --depth=0`
  returned an empty tree.
- Runtime/dependency guardrail over `package.json`, `src`, `tests`, and
  `wrangler.toml` found no matches for:
  - `agents/experimental/memory/session`
  - `@cloudflare/think`
  - `@cloudflare/ai-chat`
  - `@cloudflare/shell`
  - `workers-ai-provider`

Generated Cloudflare env types:

- `npm run cf:types` passed.
- `src/types/cloudflare-env.generated.d.ts` had no tracked diff after
  regeneration.
- The generated binding surface remained the existing MCP Agent DO, Hindsight
  containers, Graphiti container, Bootstrap workflow, KV, queues, D1, R2,
  Vectorize, Hyperdrive, Analytics, Browser, and AI bindings.

No runtime surface drift:

- No diff in `wrangler.toml`, `src/workers/mcpagent/index.ts`,
  `src/workers/mcpagent/do/McpAgent.ts`,
  `src/workers/mcpagent/do/session-store.ts`, `src/types/env.ts`,
  `src/types/cloudflare-env.generated.d.ts`,
  `src/tools/brain-memory-surface.ts`, `src/tools/canonical-memory.ts`, or
  `src/tools/memory.ts`.
- No new Cloudflare bindings, Durable Object migrations, public routes, MCP
  tools, or memory semantics were added.

---

## Validation Results

- `npm run postflight`: passed.
- Focused tests passed: 10 files, 69 tests.
  - `tests/3.2-memory-interface.test.ts`
  - `tests/1.2-auth.test.ts`
  - `tests/1.2-websocket.test.ts`
  - `tests/1.2b-mcp-agent-identity.test.ts`
  - `tests/2.4-interview.test.ts`
  - `tests/2.4-import.test.ts`
  - `tests/6.2-canonical-mcp-memory-surface.test.ts`
  - `tests/9.4-brain-memory-external-client-rollout.test.ts`
  - `tests/10.4-hyperdrive-neon-canonical-connection.test.ts`
  - `tests/10.6-production-deploy-and-hyperdrive-canary.test.ts`
- Session-store import smoke passed with the existing `mcp_agent_session`
  SQL helper shape.
- `npm test`: passed, 73 test files, 413 tests passed, 1 skipped.

Coverage notes:

- MCP route behavior is covered by Miniflare route/auth tests and the Wrangler
  bundle pass for the real `McpAgentDO.serve('/mcp')` entry.
- Durable Object identity is covered by `getMcpAgentObjectName()` /
  `getMcpAgentObjectId()` tests.
- Bootstrap behavior is covered by interview/import tests.
- Canonical memory surface and canary behavior are covered by canonical MCP
  memory, brain-memory rollout, Hyperdrive connection, and Hyperdrive canary
  tests.

---

## Wrangler Dry-Run Result

First command:

- `npx wrangler deploy --dry-run`
- Result: failed after bundle sizing because the configured container images
  require Docker CLI access even in dry-run mode.
- No deploy occurred.

Second command:

- `npx wrangler deploy --dry-run --containers-rollout=none`
- Result: passed.
- Bundle summary: `3385.62 KiB / gzip: 630.14 KiB`.
- The printed binding set matched the existing runtime surface:
  `MCPAGENT`, `HINDSIGHT`, `HINDSIGHT_WORKER`, `GRAPHITI`,
  `BOOTSTRAP_WORKFLOW`, `KV_SESSION`, queues, D1, Vectorize,
  `HYPERDRIVE_CANONICAL`, R2, Analytics, Browser, AI, and existing vars.
- No deploy occurred.

Deploy-readiness implication:

- The Worker bundle is dry-run valid.
- A later production deploy phase must either run in an environment with Docker
  available for configured containers or intentionally use
  `--containers-rollout=none` if container images are not changing.

---

## Dev Smoke Result

Live local/remote dev smoke was not feasible in this non-interactive
environment.

Local dev:

- `npx wrangler dev --local --ip 127.0.0.1 --port 8787 --log-level error`
  failed before serving.
- Blocker: Wrangler requires a local Hyperdrive emulation connection for
  `HYPERDRIVE_CANONICAL`, and no local Hyperdrive connection env var or
  `localConnectionString` is configured.
- `.dev.vars` is not present; only `.dev.vars.example` exists.

Remote dev:

- `npx wrangler dev --remote --ip 127.0.0.1 --port 8787 --log-level error`
  failed before opening the local forwarding port.
- Blocker: the workers.dev domain is behind Cloudflare Access and the
  non-interactive environment has no Access service-token credentials.
- Presence check found no `CLOUDFLARE_ACCESS_CLIENT_ID`,
  `CLOUDFLARE_ACCESS_CLIENT_SECRET`, or local Hyperdrive emulation env var.

Canary note:

- `/_canary/hyperdrive` could not be exercised through live dev because neither
  local nor remote Wrangler dev reached a serving state.
- Its unchanged behavior is covered by `tests/10.6-production-deploy-and-hyperdrive-canary.test.ts`
  and `tests/10.4-hyperdrive-neon-canonical-connection.test.ts`.

---

## Explicit No-Adoption and No-Behavior-Change Confirmations

- Did not import or instantiate `agents/experimental/memory/session`.
- Did not install or import `@cloudflare/think`.
- Did not install or import `@cloudflare/ai-chat`.
- Did not install or import `@cloudflare/shell`.
- Did not install or import `workers-ai-provider`.
- Did not adopt Sessions, Think, AI Chat, Shell, workers-ai-provider, or new
  memory/session behavior.
- Did not remove Hindsight.
- Did not add Cloudflare bindings.
- Did not add Durable Object migrations.
- Did not add public routes.
- Did not add MCP tools.
- Did not change canonical memory semantics.
- Did not deploy.

---

## Secret Hygiene

Local secret scan checked `.omx/logs` plus `SESSION_LOG.md` if present.

- Files scanned: 14
- Exact User-scope Cloudflare token hits: 0
- PostgreSQL URL-shaped string hits: 0
- Redacted files: 0

No token values or DB URLs were printed.

---

## Production Deploy Phase Readiness

HAETSAL is ready for a later explicit production deploy phase from the code,
package, type, test, and Worker bundle perspectives.

Known deploy-phase prerequisites:

- Do not deploy from this phase.
- Ensure Docker is available if the deploy phase updates configured container
  images.
- If containers are unchanged, the deploy phase can deliberately use
  `--containers-rollout=none` after confirming that is the intended deploy
  posture.
- Provide Access service-token credentials if a later phase wants live remote
  dev smoke against the Access-protected workers.dev domain.
- Provide local Hyperdrive emulation config if a later phase wants live local
  Wrangler dev smoke.

---

## As-Built Record

Completed: 2026-06-02

Implemented:

- Consulted the Cloudflare and Agents SDK skills.
- Fetched `origin`.
- Confirmed `HEAD == origin/master` at
  `f451c254218c85e8c13e9860f690ad940b6399b2`.
- Loaded the Windows User-scope Cloudflare token into relevant command
  processes without printing it.
- Read the required 10.12 and 10.10 completed specs, `package.json`,
  `wrangler.toml`, MCP agent entry and Durable Object files, session store,
  manual/generated Env types, and focused tests.
- Confirmed installed packages are `agents@0.13.3`, `zod@4.4.3`, and
  `ai@6.0.116`.
- Confirmed Think, AI Chat, Shell, and workers-ai-provider packages are not
  installed.
- Regenerated Cloudflare env types and confirmed no generated tracked diff.
- Ran postflight successfully.
- Ran focused runtime/canary/bootstrap/canonical-memory tests successfully.
- Ran a session-store import smoke successfully.
- Ran the full test suite successfully.
- Ran Wrangler deploy dry-run. Plain dry-run was blocked by local Docker
  availability for configured containers; dry-run with
  `--containers-rollout=none` passed without deploying.
- Attempted local and remote Wrangler dev smoke without deploying and recorded
  precise blockers.
- Confirmed no no-adoption guardrail matches in `package.json`, `src`, `tests`,
  or `wrangler.toml`.
- Confirmed no diff in runtime binding, route, MCP tool, Env, or memory files.
- Secret-scanned `.omx/logs` and `SESSION_LOG.md` targets without printing
  secrets.
- Created this completed spec:
  `specs/completed/10.13-post-upgrade-runtime-smoke-and-deploy-readiness.md`.
