# Spec: Production Deploy and Post-Upgrade Canary

**Phase:** 10 | **Session:** 10.14 | **Priority:** P0
**Track:** Post-Hindsight Cloudflare/Open Brain Roadmap
**Implementation Tree:** `C:\Users\matth\Documents\HAETSAL OS`
**Baseline Branch:** `master`
**Depends on:** `10.13` Post-Upgrade Runtime Smoke and Deploy-Readiness Validation

---

## What We Did

Run a controlled production deploy after the Zod 4 and current Cloudflare
Agents SDK upgrade, then prove the deployed Worker still resolves the canonical
Hyperdrive binding through the existing read-only canary.

This phase did not adopt Agents Sessions, Think, AI Chat, Shell,
workers-ai-provider, new memory/session behavior, or any Hindsight removal.

---

## Starting Baseline

Checked on 2026-06-02 from `C:\Users\matth\Documents\HAETSAL OS`.

- `git fetch origin` completed.
- `HEAD` equals `origin/master`.
- Both resolve to `bca5f400a517ed330087426392ea50e544698a48`.
- `bca5f40` is an ancestor of `HEAD`.
- The Windows User-scope `CLOUDFLARE_API_TOKEN` was loaded into relevant
  command processes without printing the token.
- The requested User-scope `CLOUDFLARE_ACCESS_CLIENT_ID` and
  `CLOUDFLARE_ACCESS_CLIENT_SECRET` variables were absent.
- User-scope `CF_ACCESS_CLIENT_ID` and `CF_ACCESS_CLIENT_SECRET` were present
  and were validated as an alternate Access service-token pair for the
  Access-protected production canary.
- Pre-existing `.omx` runtime state, local reference clones, and planning-doc
  noise were ignored for deploy purposes and left unstaged; three
  pre-existing `.omx` log files were later redacted during the secret hygiene
  pass.

---

## Preflight Results

Package surface:

- `npm ls agents zod ai --depth=0 --json` confirmed:
  - `agents@0.13.3`
  - `zod@4.4.3`
  - `ai@6.0.116`, still AI SDK v6

Excluded packages and imports:

- `npm ls @cloudflare/think @cloudflare/ai-chat @cloudflare/shell workers-ai-provider --depth=0`
  returned an empty tree; none are installed.
- Runtime/dependency guardrail over `package.json`, `src`, `tests`, and
  `wrangler.toml` found no matches for:
  - `agents/experimental/memory/session`
  - `@cloudflare/think`
  - `@cloudflare/ai-chat`
  - `@cloudflare/shell`
  - `workers-ai-provider`
- The ignored local `package-lock.json` contains `@cloudflare/ai-chat` only as
  optional peer-dependency metadata for `agents`; it is not installed or
  imported.

Validation commands:

- `npm run postflight`: passed with `All checks passed - no violations found.`
- `npm test`: passed, 73 test files, 413 tests passed, 1 skipped.
- `npx wrangler deploy --dry-run --containers-rollout=none`: passed.
  - Bundle summary: `3385.62 KiB / gzip: 630.14 KiB`.
  - Dry-run listed the existing runtime surface, including
    `env.HYPERDRIVE_CANONICAL (facdb64643d74ed2b3a3b90e7a4e2071)`.
  - Dry-run exited without deploying.

Hyperdrive metadata:

- `npx wrangler hyperdrive get facdb64643d74ed2b3a3b90e7a4e2071` was readable.
- Safe parsed fields confirmed:
  - `id`: `facdb64643d74ed2b3a3b90e7a4e2071`
  - `name`: `haetsal-canonical-neon`
  - `caching.disabled`: `true`
- No database URL or token value was printed.

Access canary path:

- Exact requested `CLOUDFLARE_ACCESS_CLIENT_ID` and
  `CLOUDFLARE_ACCESS_CLIENT_SECRET` variables were not present.
- Alternate User-scope `CF_ACCESS_CLIENT_ID` and `CF_ACCESS_CLIENT_SECRET`
  were present and valid for Cloudflare Access service-token headers.
- Pre-deploy `HEAD https://haetsalos.specialdarksystems.com/_canary/hyperdrive`
  with those Access headers returned `204` with `Cache-Control: no-store`.
- A second User-scope `CF_ACCESS_CLIENT_ID_2` / `CF_ACCESS_CLIENT_SECRET_2`
  pair was present but returned `401`; it was not used for deployment canaries.

---

## Deploy Result

Deploy was performed because every preflight gate passed and a valid
Access-protected production canary path was available.

Command:

```powershell
npx wrangler deploy --containers-rollout=none
```

Result:

- Deploy passed.
- Bundle summary: `3385.62 KiB / gzip: 630.14 KiB`.
- Worker startup time: `67 ms`.
- Deployed URL from Wrangler: `https://the-brain.ct-trading-bot1.workers.dev`.
- Current Version ID: `4209df4d-6471-4e42-8434-70c6b91d09ff`.
- Wrangler output showed upload, trigger deployment, queues, schedules, and the
  bootstrap workflow deployment completing without obvious production errors.
- Container images were not rolled; deployment used
  `--containers-rollout=none`.

---

## Post-Deploy Canary Results

Required canary:

- `HEAD https://haetsalos.specialdarksystems.com/_canary/hyperdrive`
  with Access service-token headers returned `204`.
- Response included `Cache-Control: no-store`.

Additional safe smokes:

- `HEAD https://the-brain.ct-trading-bot1.workers.dev/_canary/hyperdrive`
  with Access service-token headers returned `204`.
- Response included `Cache-Control: no-store`.
- `GET https://haetsalos.specialdarksystems.com/` with Access service-token
  headers returned `404`.
- `GET https://haetsalos.specialdarksystems.com/mcp` with Access service-token
  headers and no app JWT returned `406`.

The additional smokes were non-mutating reachability/auth-shape checks. They
did not send an app JWT, did not invoke MCP tools, did not read memory, and did
not write memory.

---

## Explicit Guardrail Confirmations

- No container image rollout was performed.
- Did not install, import, instantiate, or adopt
  `agents/experimental/memory/session`.
- Did not install, import, instantiate, or adopt `@cloudflare/think`.
- Did not install, import, instantiate, or adopt `@cloudflare/ai-chat`.
- Did not install, import, instantiate, or adopt `@cloudflare/shell`.
- Did not install, import, instantiate, or adopt `workers-ai-provider`.
- Did not adopt Sessions, Think, AI Chat, Shell, workers-ai-provider, or new
  memory/session behavior.
- Did not add Cloudflare bindings.
- Did not add Durable Object migrations.
- Did not add public routes.
- Did not add MCP tools.
- Did not change canonical memory semantics.
- Did not remove Hindsight.

---

## Secret Hygiene

Final local scan checked the intended completed spec, `.omx/logs`, any
`SESSION_LOG.md`, and 2026-06-02 Wrangler logs.

- Files scanned: 92
- Initial exact secret hits: 3, all for the pre-existing
  `CF_ACCESS_CLIENT_ID` value in April `.omx` turn logs.
- Redacted files: 3 pre-existing local `.omx` log files.
- Final exact secret hits: 0
- Final PostgreSQL URL-shaped string hits: 0

No token values, Access service-token values, or database URLs were printed in
the command summaries above.

---

## As-Built Record

Completed: 2026-06-02

Implemented:

- Consulted the Ralph, Cloudflare, Agents SDK, and Wrangler skills.
- Created a Ralph context snapshot under `.omx/context` and left runtime state
  unstaged.
- Redacted three pre-existing local `.omx` log files that contained the exact
  `CF_ACCESS_CLIENT_ID` value; these runtime log changes were left unstaged.
- Fetched `origin`.
- Confirmed `HEAD == origin/master` at
  `bca5f400a517ed330087426392ea50e544698a48`.
- Loaded the Windows User-scope Cloudflare API token into command processes
  without printing it.
- Loaded and checked the requested Access service-token environment names;
  they were absent.
- Identified and validated the alternate User-scope `CF_ACCESS` service-token
  pair as the valid Access canary path.
- Read the required 10.13, 10.12, and 10.6 completed specs, `package.json`,
  `wrangler.toml`, `src/workers/mcpagent/index.ts`, and
  `tests/10.6-production-deploy-and-hyperdrive-canary.test.ts`.
- Confirmed installed packages are `agents@0.13.3`, `zod@4.4.3`, and
  `ai@6.0.116`.
- Confirmed excluded package/runtime surfaces are not installed or imported.
- Ran `npm run postflight` successfully.
- Ran `npm test` successfully.
- Ran `npx wrangler deploy --dry-run --containers-rollout=none` successfully.
- Confirmed Hyperdrive config `facdb64643d74ed2b3a3b90e7a4e2071` is readable as
  `haetsal-canonical-neon` and has caching disabled.
- Deployed production with `npx wrangler deploy --containers-rollout=none`.
- Captured deployed URL and Version ID.
- Ran required and supplemental post-deploy canaries successfully.
- Created this completed spec:
  `specs/completed/10.14-production-deploy-and-post-upgrade-canary.md`.
