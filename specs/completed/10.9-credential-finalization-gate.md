# Spec: Credential Finalization Gate

**Phase:** 10 | **Session:** 10.9 | **Priority:** P0
**Track:** Post-Hindsight Cloudflare/Open Brain Roadmap
**Implementation Tree:** `C:\Users\matth\Documents\HAETSAL OS`
**Baseline Branch:** `master`
**Depends on:** `10.8` Cloudflare Token Rotation And Local Secret Scrub

---

## What We're Building

This session is the final credential gate before moving to Agents Sessions /
Think work. It confirms whether the external Cloudflare and provider-side
credential work left open by 10.8 is actually complete.

No runtime behavior should change.

---

## Current Baseline

- Local `HEAD` must equal `origin/master`.
- Local `HEAD` must be `28be12c` or newer.
- `wrangler.toml` binds `HYPERDRIVE_CANONICAL` to
  `facdb64643d74ed2b3a3b90e7a4e2071`.
- Token values and database URLs must not be printed into docs, command
  summaries, commits, or final output.

---

## Brain Laws Check

### Law 1: One Public Face

No public surface is added or changed.

### Law 2: Zero-Knowledge

No memory content is read or written. Database credential validation, if any,
must be limited to connection/authentication status and must not print the URL
or query user data.

### Law 3: Agent Write Policy

No agent memory writes, memory type behavior, or write policy paths change.

### Law 4: Post-Hindsight Direction

No Hindsight removal or Hindsight runtime change is in scope.

### Law 5: Canonical Cloudflare Data Path

The canonical Cloudflare data path remains the existing
`HYPERDRIVE_CANONICAL` binding. This spec only verifies credential readiness
around that binding.

---

## Scope

### In Scope

1. Verify `HEAD == origin/master` and `HEAD` is `28be12c` or newer.
2. Do not print token values or database URLs.
3. Verify active `CLOUDFLARE_API_TOKEN`:
   - `/user/tokens/verify` succeeds.
   - Can read Hyperdrive config `facdb64643d74ed2b3a3b90e7a4e2071`.
   - Can run Wrangler deploy dry-run.
4. If Hyperdrive read still fails, stop and report that the token still needs
   Account `Hyperdrive Read`.
5. Confirm provider-side canonical DB password rotation has been completed, or
   record the exact remaining manual provider action.
6. Re-scan untracked `.omx/logs` for token-like strings and Postgres URLs.
7. Run `npm run postflight`.
8. Run `npx wrangler deploy --dry-run --containers-rollout=none`.
9. Append this As-Built record, move this spec to `specs/completed`, commit,
   and push.

### Out of Scope

- No runtime code changes.
- No live deploy.
- No Hindsight removal.
- No memory behavior changes.

---

## Acceptance Criteria

- [x] `pwd` is `C:\Users\matth\Documents\HAETSAL OS`.
- [x] `HEAD == origin/master`.
- [x] `HEAD` is `28be12c` or newer.
- [x] No token values or database URLs are printed.
- [x] Active `CLOUDFLARE_API_TOKEN` verifies itself.
- [x] Active `CLOUDFLARE_API_TOKEN` can read Hyperdrive config
      `facdb64643d74ed2b3a3b90e7a4e2071`, or the exact permission gap is
      recorded.
- [x] Hyperdrive read failure, if still present, is reported as requiring
      Account `Hyperdrive Read`.
- [x] Active `CLOUDFLARE_API_TOKEN` can run Wrangler deploy dry-run.
- [x] Provider-side canonical DB password rotation state is recorded without
      printing the database URL.
- [x] Untracked `.omx/logs` are scanned for token-like strings and Postgres
      URLs.
- [x] `npm run postflight` passes.
- [x] `npx wrangler deploy --dry-run --containers-rollout=none` passes.
- [x] No runtime code changes are made.
- [x] Spec is moved to `specs/completed`.

---

## As-Built Record

Stopped: 2026-06-01

Result:

- Verified local execution from `C:\Users\matth\Documents\HAETSAL OS`.
- Fetched `origin/master`.
- Confirmed local `HEAD` equals `origin/master`, and both resolved to
  `28be12c3c665a8ffc336e1277324e7da35031237`.
- Confirmed `28be12c` is an ancestor of `HEAD`.
- Confirmed `wrangler.toml` still binds `HYPERDRIVE_CANONICAL` to
  `facdb64643d74ed2b3a3b90e7a4e2071`.
- No runtime source files were changed.
- No live deploy was performed.
- No token values or database URLs were printed into this spec.

Cloudflare token findings:

- The active process has `CLOUDFLARE_API_TOKEN` set.
- `/user/tokens/verify` reports the active token is valid and active.
- The active token can list the account visible to the Cloudflare API.
- The active token cannot read Hyperdrive config
  `facdb64643d74ed2b3a3b90e7a4e2071`.
- Direct API `GET /accounts/{account_id}/hyperdrive/configs/{hyperdrive_id}`
  failed with HTTP `403`.
- Stop condition triggered: the active token still needs Account
  `Hyperdrive Read` scoped to the account containing Hyperdrive config
  `facdb64643d74ed2b3a3b90e7a4e2071`.

Stopped steps:

- Did not continue to provider-side canonical DB password rotation
  confirmation because the Hyperdrive read gate failed first.
- Did not re-scan untracked `.omx/logs` in this session because the
  Hyperdrive read gate failed first.
- Did not run `npm run postflight` in this session because the Hyperdrive read
  gate failed first.
- Did not run `npx wrangler deploy --dry-run --containers-rollout=none` in
  this session because the Hyperdrive read gate failed first.
- Did not move this spec to `specs/completed`, commit, or push because the
  final credential gate is not complete.

Exact remaining Cloudflare action:

- Create, roll, or update the active process token in Cloudflare Dashboard >
  My Profile > API Tokens so it is scoped to the target account and includes
  Account `Hyperdrive Read` in addition to the existing deployment permission.
  Then update the local `CLOUDFLARE_API_TOKEN` value and rerun this spec.

Carried-forward provider action from 10.8:

- In the database provider dashboard/API, rotate the password for the canonical
  database role used by Hyperdrive, update Hyperdrive config
  `facdb64643d74ed2b3a3b90e7a4e2071` with the new origin password or
  connection string, and record provider-side confirmation that the previous
  password was revoked. This was not rechecked in 10.9 because the Cloudflare
  Hyperdrive read gate failed first.

---

## Rerun Record

Stopped: 2026-06-01 12:00:26 -07:00

Result:

- Refreshed `origin/master` and reconfirmed local `HEAD` equals
  `origin/master`; both resolved to
  `28be12c3c665a8ffc336e1277324e7da35031237`.
- Reconfirmed `28be12c` is an ancestor of `HEAD`.
- Reconfirmed `wrangler.toml` binds `HYPERDRIVE_CANONICAL` to
  `facdb64643d74ed2b3a3b90e7a4e2071`.
- Confirmed no token values or database URLs were printed into this spec.
- Confirmed the process-scope `CLOUDFLARE_API_TOKEN` verifies as active.
- The process-scope token still cannot read Hyperdrive config
  `facdb64643d74ed2b3a3b90e7a4e2071`; the direct Cloudflare API read returned
  HTTP `403`.
- Found that the User-scope `CLOUDFLARE_API_TOKEN` differs from the
  process-scope token, but Cloudflare rejects the User-scope value during
  `/user/tokens/verify` with HTTP `401`.
- Basic local normalization of the User-scope value did not produce a token
  Cloudflare accepts.

Stopped steps:

- Did not continue to provider-side canonical DB password rotation
  confirmation because the Hyperdrive read gate failed first.
- Did not re-scan untracked `.omx/logs` in this rerun because the Hyperdrive
  read gate failed first.
- Did not run `npm run postflight` in this rerun because the Hyperdrive read
  gate failed first.
- Did not run `npx wrangler deploy --dry-run --containers-rollout=none` in
  this rerun because the Hyperdrive read gate failed first.
- Did not move this spec to `specs/completed`, commit, or push because the
  final credential gate is not complete.

Exact remaining Cloudflare action:

- Ensure the active token available to this Codex process is the newly rotated
  Cloudflare API token, that the environment variable contains only the raw
  API token value, and that `/user/tokens/verify` accepts it.
- Ensure that accepted token is scoped to the target account and includes
  Account `Hyperdrive Read` for Hyperdrive config
  `facdb64643d74ed2b3a3b90e7a4e2071`.

Provider-side canonical DB password rotation status:

- Not independently rechecked in this rerun because the Cloudflare Hyperdrive
  read gate failed first. The carried-forward provider action remains: confirm
  in the database provider dashboard/API that the canonical database role
  password used by Hyperdrive has been rotated, that Hyperdrive config
  `facdb64643d74ed2b3a3b90e7a4e2071` uses the replacement credential, and that
  the previous password has been revoked.

---

## Final As-Built Record

Completed: 2026-06-01

Result:

- Set the provided Cloudflare API token as the Windows User-scope
  `CLOUDFLARE_API_TOKEN` for the development phase.
- Re-ran the gate using the User-scope token loaded into each child command
  process.
- Refreshed `origin/master` and reconfirmed local `HEAD` equals
  `origin/master`; both resolved to
  `28be12c3c665a8ffc336e1277324e7da35031237`.
- Reconfirmed `28be12c` is an ancestor of `HEAD`.
- Reconfirmed `wrangler.toml` binds `HYPERDRIVE_CANONICAL` to
  `facdb64643d74ed2b3a3b90e7a4e2071`.
- `/user/tokens/verify` reports the User-scope token is valid and active.
- The active token can list the target Cloudflare account.
- The active token can read Hyperdrive config
  `facdb64643d74ed2b3a3b90e7a4e2071`.
- The Hyperdrive config read confirmed the config name
  `haetsal-canonical-neon` and that caching is disabled.
- `npm run postflight` passed.
- `npx wrangler deploy --dry-run --containers-rollout=none` passed and exited
  with `--dry-run: exiting now`.
- No live deploy was performed.
- No runtime source files were changed.
- No Hindsight removal or memory behavior changes were made.
- No token values or database URLs were printed into this spec.

Log hygiene:

- The first untracked `.omx/logs` scan found token-shaped strings in
  `.omx/logs/turns-2026-06-01.jsonl` and no Postgres URLs.
- Redacted token-shaped strings in the untracked `.omx/logs` files.
- Re-scanned untracked `.omx/logs`; the final scan passed with zero
  token-like strings and zero Postgres URLs across two untracked log files.

Provider-side canonical DB password rotation status:

- User clarified after completion that provider-side passwords and credentials
  should remain active and stable until the development phase is over.
- No provider-side password or credential rotation was attempted or required
  for this development-phase gate.
- The Hyperdrive config read proved Cloudflare can read the canonical
  Hyperdrive configuration metadata; it did not require printing or using a
  database URL.
- No local Neon/provider API credential is available in this environment for
  provider-side rotation auditing. Even if a database connection string exists
  in a Worker secret or Hyperdrive origin config, that would prove connection
  availability rather than provider-side password revocation.
- Post-development follow-up: rotate and revoke provider-side database
  credentials when the development phase is complete.

---

## Validation Review

Rechecked on 2026-06-02 from `C:\Users\matth\Documents\HAETSAL OS`.

Validated:

- Local `HEAD` equals `origin/master` at
  `939ed8fa02fd1a13c5a5981080c7ca4b0b369665`.
- The User-scope `CLOUDFLARE_API_TOKEN` is present and differs from the older
  process-scope token visible to this validation process.
- Loading the User-scope token into child commands makes
  `/user/tokens/verify` pass.
- The User-scope token can read Hyperdrive config
  `facdb64643d74ed2b3a3b90e7a4e2071`.
- The Hyperdrive config read confirmed:
  - config id matches `facdb64643d74ed2b3a3b90e7a4e2071`
  - config name is `haetsal-canonical-neon`
  - caching is disabled
- `npm run postflight` passed.
- `npx wrangler deploy --dry-run --containers-rollout=none` passed with the
  User-scope token explicitly loaded and listed
  `env.HYPERDRIVE_CANONICAL (facdb64643d74ed2b3a3b90e7a4e2071)`.

Local log hygiene:

- A value-blind scan of untracked `.omx/logs` files initially found
  token-shaped candidates in the untracked turn logs, including the User-scope
  Cloudflare token.
- Non-current token-shaped candidates did not validate as active Cloudflare
  tokens.
- All token-shaped candidates and Postgres URLs were redacted in place from
  untracked `.omx/logs` files.
- A final value-blind scan of untracked `.omx/logs` files passed with zero
  token-shaped candidates and zero Postgres URLs.

Conclusion:

- 10.9 is complete for the development phase.
- Future Cloudflare commands should explicitly load the User-scope token, or
  the parent shell/process should be restarted so `CLOUDFLARE_API_TOKEN`
  inherits the rotated value.
- Provider-side database credential rotation remains intentionally deferred
  until after the development phase, per user direction.
