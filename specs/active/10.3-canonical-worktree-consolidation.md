# Spec: Canonical Worktree Consolidation

**Phase:** 10 | **Session:** 10.3 | **Priority:** P0
**Track:** Post-Hindsight Cloudflare/Open Brain Roadmap
**Depends on:** `10.2` Cloudflare runtime currency upgrade
**Implementation Branch:** `codex/post-hindsight-cloudflare-reconciliation`
**Implementation Tree:** `C:\Users\matth\Documents\HAETSAL OS 11.4 deploy`

---

## What We're Building

Resolve the HAETSAL workspace identity confusion before continuing with
Hyperdrive or Hindsight removal.

This is not a feature implementation session. It is a consolidation and
handoff session that makes one thing explicit:

- which local checkout is the active HAETSAL OS implementation tree
- which folders are historical, alternate worktrees, or separate projects
- what should be renamed, archived, or left alone
- what fresh-session prompts should use going forward

---

## Why This Session Exists

The local folders are misleading:

- `C:\Users\matth\Documents\HAETSAL OS`
- `C:\Users\matth\Documents\HAETSAL OS 11.4 deploy`
- `C:\Users\matth\Documents\HAETSAL`

The `11.4 deploy` name sounds old, but Git evidence shows it is a worktree of
the same `HAETSAL OS` repository and contains later runtime commits than
`origin/master`.

Before adding Hyperdrive or removing Hindsight, we should stop relying on a
folder name that invites mistakes.

---

## Current Evidence To Verify

Verify these facts at execution time:

- `C:\Users\matth\Documents\HAETSAL OS 11.4 deploy` is a Git worktree of the
  same `HAETSAL OS` repository.
- It is on `codex/post-hindsight-cloudflare-reconciliation`.
- It includes:
  - `d6cc009 Implement 11.4 compilation triggers`
  - `22e9cfd chore: update cloudflare runtime baseline`
- `origin/master` is an ancestor of the 11.4 candidate lineage.
- `C:\Users\matth\Documents\HAETSAL` is not the same repo root. Its nested
  `haetsal-agent` repo is a different GitHub repo and appears older.
- A temp detached worktree may exist under
  `C:\Users\matth\AppData\Local\Temp\haetsal-deploy-*`; it is not the active
  project.

---

## Scope

### In Scope

1. Audit local HAETSAL folders and Git worktrees.
2. Produce:
   - `docs/implementation-plans/canonical-worktree-consolidation-report.md`
3. Declare the canonical active implementation tree for the next sessions.
4. Recommend a safe physical cleanup path:
   - no rename yet
   - rename later with explicit user approval
   - or use `git worktree move` after preserving dirty work
5. Update this spec with an As-Built record.

### Out of Scope

- Deleting folders.
- Removing Git worktrees.
- Renaming `HAETSAL OS` or `HAETSAL OS 11.4 deploy`.
- Merging branches.
- Pushing to GitHub.
- Removing Hindsight.
- Adding Hyperdrive.
- Adding AI Search.
- Changing runtime code.

---

## Recommended Default

Default recommendation:

- Treat `C:\Users\matth\Documents\HAETSAL OS 11.4 deploy` on
  `codex/post-hindsight-cloudflare-reconciliation` as the active implementation
  tree for now.
- Do not physically rename or delete either worktree in this session.
- Update future prompts to use the 11.4 deploy path until the user explicitly
  approves a rename/consolidation operation.

Why this default:

- it avoids destructive filesystem moves
- it preserves dirty runtime work
- it keeps the verified 10.1 and 10.2 commits on the active branch
- it prevents accidental implementation on stale `master`

---

## Report Template

Create `docs/implementation-plans/canonical-worktree-consolidation-report.md`
with these sections:

### 1. Executive Decision

- canonical active tree
- canonical active branch
- latest relevant commit
- whether there is one project or multiple projects

### 2. Local Folder Inventory

For each local HAETSAL-looking folder:

- path
- git repo or not
- branch
- latest commit/date
- relationship to `HAETSAL OS`
- disposition: active, legacy/reference, separate project, temp/ignore

### 3. Why `11.4 deploy` Exists

Explain, using Git evidence, whether it is:

- a separate project
- a linked worktree
- a deploy candidate worktree
- a temporary deployment checkout

### 4. Keep / Rename / Archive Recommendation

Recommend one of:

- keep current physical names for now and always prompt against the active path
- later rename with `git worktree move`
- archive old `master` worktree after preserving untracked reference folders

Do not execute the rename in this session.

### 5. Next Spec

If canonical tree is confirmed, recommend:

- `10.4-hyperdrive-neon-canonical-connection.md`

If canonical tree is not confirmed, recommend the blocker and stop.

---

## Verification Commands

Run read-only checks:

```powershell
git worktree list
git status --short --branch
git log -8 --oneline --decorate --date=iso
git branch -avv --sort=-committerdate
git merge-base --is-ancestor origin/master codex/11-4-deploy-candidate
git merge-base --is-ancestor codex/11-4-deploy-candidate codex/post-hindsight-cloudflare-reconciliation
```

Also inspect:

```powershell
Get-ChildItem -Path "C:\Users\matth\Documents" -Directory | Where-Object { $_.Name -match "HAETSAL|Haetsal|haetsal" }
```

No tests are required unless runtime files are changed. Runtime files should not
be changed.

---

## Fresh Session Prompt

```text
You are working in C:\Users\matth\Documents\HAETSAL OS 11.4 deploy.

Please execute only this spec:

specs/active/10.3-canonical-worktree-consolidation.md

Do not rename, delete, move, reset, merge, or push anything. Do not change
runtime code. This is a read-only audit plus documentation session.

Audit the local HAETSAL folders and Git worktrees, create
docs/implementation-plans/canonical-worktree-consolidation-report.md, append the
As-Built record, and stop with the recommended canonical path for future work.
```

---

## Acceptance Criteria

- `docs/implementation-plans/canonical-worktree-consolidation-report.md` exists.
- The report explains whether `HAETSAL OS 11.4 deploy` is a second project or a
  worktree.
- The report identifies the active implementation path and branch.
- The report explains the status of `C:\Users\matth\Documents\HAETSAL`.
- The report recommends whether to keep, rename, or archive the extra worktree.
- No folders are renamed, deleted, moved, reset, merged, or pushed.
- No runtime code is changed.
- This spec has an As-Built record appended.

---

## As-Built Record

_To be completed by the execution session._
